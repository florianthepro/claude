# Archiv: Single-File-Version

Dies ist die ursprüngliche, komplett eigenständige Single-File-Fassung von
Nexus (nur `index.php`). Sie bleibt hier als Referenz erhalten. Die aktive,
modulare Weiterentwicklung liegt im Wurzelverzeichnis des Repos.

