# Dieses Verzeichnis ist intern und wird niemals über das Web ausgeliefert.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
