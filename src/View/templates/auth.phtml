<?php
/** @var \Stimmwerk\App $app */
/** @var callable $t */
/** @var bool $isMock */
?>
<h1><?= e($t('auth.title')) ?></h1>
<div class="auth-grid">
  <section class="card">
    <p><?= e($t('auth.intro')) ?></p>
    <h2><?= e($t('auth.how_title')) ?></h2>
    <ol class="steps">
      <li><?= e($t('auth.step_1')) ?></li>
      <li><?= e($t('auth.step_2')) ?></li>
      <li><?= e($t('auth.step_3')) ?></li>
    </ol>
    <p class="muted"><?= e($t('auth.privacy')) ?></p>
  </section>

  <?php if ($isMock): ?>
  <section class="card mock-box">
    <h2><?= e($t('auth.mock_title')) ?></h2>
    <p class="muted"><?= e($t('auth.mock_warning')) ?></p>
    <form method="post" action="<?= base_path() ?>/auth/mock" class="form-stack">
      <?= $app->csrf->field() ?>
      <button type="submit" name="action" value="generate" class="btn btn-primary"><?= e($t('auth.mock_generate')) ?></button>
    </form>
    <p class="muted auth-or"><?= e($t('auth.mock_or')) ?></p>
    <form method="post" action="<?= base_path() ?>/auth/mock" class="form-stack">
      <?= $app->csrf->field() ?>
      <label>
        <span><?= e($t('auth.mock_secret')) ?></span>
        <input type="text" name="card_secret" minlength="8" maxlength="128" required autocomplete="off">
        <small class="muted"><?= e($t('auth.mock_secret_help')) ?></small>
      </label>
      <div><button type="submit" name="action" value="login" class="btn btn-outline"><?= e($t('auth.mock_login')) ?></button></div>
    </form>
  </section>
  <?php else: ?>
  <section class="card">
    <p class="muted"><?= e($t('auth.not_configured')) ?></p>
  </section>
  <?php endif; ?>
</div>
